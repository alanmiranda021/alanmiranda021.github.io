
/* AM // OMNI INTERFACE v2 */
(()=>{
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const root=document.body;
const layer=document.createElement('div'); layer.className='omni-layer'; layer.innerHTML='<canvas id="omniCanvas"></canvas><div class="omni-glow"></div><div class="omni-noise"></div><div class="omni-scan"></div><div class="omni-vignette"></div>'; root.appendChild(layer);
const frame=document.createElement('div'); frame.className='omni-frame'; root.appendChild(frame);
const hud=document.createElement('div'); hud.className='omni-hud'; hud.innerHTML='<div class="a">AM // OMNI INTERFACE<br><strong>FIELD NODE / ONLINE</strong></div><div class="b">SESSION <strong id="omSession">ACTIVE</strong><br>MODE <strong>EXPLORATION</strong></div><div class="c">INPUT <strong id="omInput">TRACKING</strong><br>RENDER <strong>CANVAS / 2D</strong></div><div class="d">LOCAL <strong id="omClock">--:--:--</strong><br>SCROLL <strong id="omScroll">000%</strong></div>'; root.appendChild(hud);
const tele=document.createElement('div'); tele.className='omni-telemetry'; tele.innerHTML='<b>SYSTEM TELEMETRY</b><span>CPU <i id="omCpu">--%</i></span><span>LINK <i id="omLink">--ms</i></span><span>MEM <i id="omMem">--%</i></span><span>FLOW <i id="omFlow">--</i></span>'; root.appendChild(tele);
const cursor=document.createElement('div'); cursor.className='omni-cursor'; root.appendChild(cursor);
const intro=document.createElement('div'); intro.className='omni-intro'; intro.innerHTML='<div class="omni-intro-inner"><div class="omni-intro-title">ALAN MIRANDA<br>OMNI SYSTEM</div><div class="omni-intro-sub">INITIALIZING INTERFACE / SYNCHRONIZING MODULES</div><div class="omni-bar"></div><div class="omni-readout"><span>CORE</span><span id="omInit">00%</span></div></div>'; root.appendChild(intro); setTimeout(()=>intro.classList.add('done'),2100);
// command palette
const cmd=document.createElement('div'); cmd.className='omni-command'; cmd.innerHTML='<div class="omni-box"><div class="omni-head"><span>COMMAND CENTER / NAVIGATOR</span><span>ESC</span></div><input id="omSearch" placeholder="Buscar seção, projeto ou comando..."><div class="omni-results" id="omResults"></div></div>'; root.appendChild(cmd);
const results=$('#omResults',cmd); const navs=$$('a[href^="#"],a[href*="index.html#"]').filter(a=>a.textContent.trim());
const unique=[]; navs.forEach(a=>{const key=a.getAttribute('href')+'|'+a.textContent.trim(); if(!unique.some(x=>x.k===key)) unique.push({k:key,a})});
function render(q=''){results.innerHTML=''; unique.filter(x=>x.a.textContent.toLowerCase().includes(q.toLowerCase())||x.a.getAttribute('href').toLowerCase().includes(q.toLowerCase())).slice(0,18).forEach(x=>{const b=document.createElement('button');b.textContent='→ '+x.a.textContent.trim().replace(/\s+/g,' ');b.onclick=()=>{cmd.classList.remove('open');x.a.click()};results.appendChild(b)})} render();
const search=$('#omSearch',cmd); search.oninput=()=>render(search.value); addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();cmd.classList.add('open');search.focus()} if(e.key==='Escape'){cmd.classList.remove('open')}}); $('.command')?.addEventListener('click',e=>{if($('.command').tagName==='BUTTON'){e.preventDefault();cmd.classList.add('open');search.focus()}});
// pointer field
addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--omx',e.clientX+'px');document.documentElement.style.setProperty('--omy',e.clientY+'px');cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px';const hit=e.target.closest('a,button,.build-card,.skill-card,.omni-sensor'); if(hit){cursor.style.width='300px';cursor.style.height='300px'}else{cursor.style.width='220px';cursor.style.height='220px'}});
// 3D tilt without dependencies
$$('.build-card,.skill-card,.omni-panel,.portrait,.stack-map').forEach(el=>{el.addEventListener('pointermove',e=>{if(matchMedia('(max-width:900px)').matches)return;const r=el.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${(-y*3).toFixed(2)}deg) rotateY(${(x*3).toFixed(2)}deg) translateY(-3px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
// canvas neural field
const c=$('#omniCanvas'),ctx=c.getContext('2d'); let W,H,pts=[]; function resize(){W=c.width=innerWidth*devicePixelRatio;H=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';pts=Array.from({length:Math.min(95,Math.max(45,Math.floor(innerWidth/16)))},()=>({x:Math.random()*W,y:Math.random()*H,vx:(Math.random()-.5)*.18*devicePixelRatio,vy:(Math.random()-.5)*.18*devicePixelRatio,r:(.7+Math.random()*1.5)*devicePixelRatio}))} addEventListener('resize',resize); resize();
function draw(){ctx.clearRect(0,0,W,H);const mx=parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--omx'))*devicePixelRatio||W*.5,my=parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--omy'))*devicePixelRatio||H*.5; for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>W)p.vx*=-1;if(p.y<0||p.y>H)p.vy*=-1;const dx=p.x-mx,dy=p.y-my,d=Math.hypot(dx,dy);if(d<250*devicePixelRatio){p.x-=dx*.0007;p.y-=dy*.0007}ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(94,234,212,.42)';ctx.fill();} for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const a=pts[i],b=pts[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<150*devicePixelRatio){ctx.strokeStyle=`rgba(34,211,238,${.075*(1-d/(150*devicePixelRatio))})`;ctx.lineWidth=devicePixelRatio;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}requestAnimationFrame(draw)} draw();
// telemetry is deliberately simulated UI feedback
let flow=0; function tick(){const now=new Date();$('#omClock').textContent=now.toLocaleTimeString('pt-BR',{hour12:false});const max=document.documentElement.scrollHeight-innerHeight;$('#omScroll').textContent=String(Math.round((scrollY/(max||1))*100)).padStart(3,'0')+'%';$('#omCpu').textContent=(18+Math.random()*31).toFixed(0)+'%';$('#omLink').textContent=(8+Math.random()*17).toFixed(0)+'ms';$('#omMem').textContent=(34+Math.random()*22).toFixed(0)+'%';$('#omFlow').textContent=(++flow%99).toString().padStart(2,'0');$('#omInit').textContent=Math.min(100,Math.round((performance.now()/2100)*100))+'%';} setInterval(tick,700); tick();
// project launch experience
$$('.build-card[data-modal]').forEach(card=>{const btn=$('button',card); if(!btn)return; btn.addEventListener('click',e=>{e.stopPropagation(); const type=card.dataset.modal; const data={aeon:['AEON','INTELLIGENT SYSTEMS','Arquitetura experimental de agentes, memória, RAG, conhecimento e orquestração.'],alpha:['ALPHA APP','DATA / EXPERIMENTATION','Laboratório de modelos, calibração, backtesting e experimentação preditiva.'],include:['INCLUDE','ROBOTICS / EDUCATION','Robótica educacional, eletrônica, prototipagem e integração hardware/software.']}[type]; if(!data)return; const overlay=document.createElement('div');overlay.className='omni-project-launch open';overlay.innerHTML=`<div class="omni-launch-box"><div class="code">${data[1]} / BUILD PREVIEW</div><h2>${data[0]}</h2><p>${data[2]}</p><div class="omni-launch-actions"><a href="projects/${type}.html">ENTRAR NO PROJETO ↗</a><button>FECHAR / ESC</button></div></div>`;document.body.appendChild(overlay);$('.omni-launch-actions button',overlay).onclick=()=>overlay.remove(); overlay.addEventListener('click',x=>{if(x.target===overlay)overlay.remove()})})});
})();
