(()=>{
const $=(s,c=document)=>c.querySelector(s), $$=(s,c=document)=>[...c.querySelectorAll(s)];
const layer=document.createElement('div');layer.className='future-layer';layer.innerHTML='<canvas id="fxCanvas"></canvas><div class="fx-noise"></div><div class="fx-vignette"></div><div class="fx-scan"></div><div class="fx-cursor"></div>';document.body.appendChild(layer);
const hud=document.createElement('div');hud.className='fx-hud';hud.innerHTML='<div class="tl">AM//SYS <b>ONLINE</b></div><div class="tr">FIELD NODE / <b>07</b><br>SECURE LINK</div><div class="bl">SCROLL VECTOR <b id="fxScroll">000</b></div><div class="br"><b id="fxClock">00:00:00</b><br>RIO / BR</div>';document.body.appendChild(hud);
['fx-c1','fx-c2','fx-c3','fx-c4'].forEach(c=>{let e=document.createElement('i');e.className='fx-corner '+c;document.body.appendChild(e)});
const read=document.createElement('div');read.className='fx-readout';read.innerHTML='<span>CORE <i>ONLINE</i></span><span>LINK <i>STABLE</i></span><span>LAT <i id="fxLat">12ms</i></span><span>LOAD <i id="fxLoad">18%</i></span><span>MODE <i>ADAPTIVE</i></span>';document.body.appendChild(read);
const enter=document.createElement('div');enter.className='fx-enter';enter.innerHTML='<div class="fx-enter-inner"><div class="fx-enter-title">ALAN MIRANDA // SYSTEMS LAB</div><div>INITIALIZING INTERFACE / CONNECTING NODES</div><div class="fx-enter-line"></div></div>';document.body.appendChild(enter);setTimeout(()=>enter.classList.add('done'),2100);
const c=$('#fxCanvas'),ctx=c.getContext('2d');let pts=[],W,H,mouse={x:innerWidth/2,y:innerHeight/2};function resize(){W=c.width=innerWidth*devicePixelRatio;H=c.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(90,Math.floor(innerWidth/15))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.18,r:Math.random()*1.4+.3}))}resize();addEventListener('resize',resize);
function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(let p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,7);ctx.fillStyle='rgba(94,234,212,.45)';ctx.fill();for(let q of pts){let dx=p.x-q.x,dy=p.y-q.y,d=Math.hypot(dx,dy);if(d<110){ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.strokeStyle=`rgba(94,234,212,${.045*(1-d/110)})`;ctx.stroke()}}}requestAnimationFrame(draw)}draw();
addEventListener('pointermove',e=>{mouse.x=e.clientX;mouse.y=e.clientY;document.documentElement.style.setProperty('--mx',e.clientX+'px');document.documentElement.style.setProperty('--my',e.clientY+'px');$('.fx-cursor').style.left=e.clientX+'px';$('.fx-cursor').style.top=e.clientY+'px';let t=e.target.closest('.btn,.build-card,.skill-card,a,button');if(t){$('.fx-cursor').style.width='320px';$('.fx-cursor').style.height='320px'}else{$('.fx-cursor').style.width='260px';$('.fx-cursor').style.height='260px'}let h=e.target.closest('.fx-hotspot');if(h){let r=h.getBoundingClientRect();h.style.setProperty('--spotx',(e.clientX-r.left)+'px');h.style.setProperty('--spoty',(e.clientY-r.top)+'px')}});
$$('.reveal,.build-card,.skill-card,.section-top,.event').forEach((e,i)=>{e.classList.add('fx-reveal');e.style.transitionDelay=Math.min(i*35,350)+'ms'});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('fx-visible')}),{threshold:.08});$$('.fx-reveal').forEach(e=>io.observe(e));
$$('.build-card,.skill-card,.terminal-box,.sys-diagram,.hmi-node,.tele-item').forEach(e=>e.classList.add('fx-glow','fx-hotspot'));
function clock(){let d=new Date();$('#fxClock').textContent=d.toLocaleTimeString('pt-BR',{hour12:false});$('#fxScroll').textContent=String(Math.round(scrollY/(document.documentElement.scrollHeight-innerHeight||1)*100)).padStart(3,'0');$('#fxLoad').textContent=Math.round(12+Math.random()*27)+'%';$('#fxLat').textContent=Math.round(8+Math.random()*18)+'ms'}setInterval(clock,1000);clock();
let cmd=document.createElement('div');cmd.className='fx-command';cmd.innerHTML='<div class="fx-command-box"><div class="fx-command-head"><span>COMMAND / NAVIGATOR</span><span>ESC</span></div><input placeholder="Digite um comando..." autofocus><div class="fx-command-list"></div></div>';document.body.appendChild(cmd);let list=$('.fx-command-list',cmd);let links=$$('a[href^="#"],a[href*="index.html#"]').filter(a=>a.textContent.trim()).slice(0,20);links.forEach(a=>{let b=document.createElement('button');b.textContent='→ '+a.textContent.trim().replace(/\s+/g,' ');b.onclick=()=>{cmd.classList.remove('open');a.click()};list.appendChild(b)});function open(){cmd.classList.add('open');$('input',cmd).focus()}addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();open()}if(e.key==='Escape')cmd.classList.remove('open')});$('.command')?.addEventListener('click',e=>{if($('.command').tagName==='BUTTON'){e.preventDefault();open()}});
// magnetic interaction for primary buttons
$$('.btn, .back, .command').forEach(el=>el.addEventListener('pointermove',e=>{let r=el.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.08,y=(e.clientY-r.top-r.height/2)*.08;el.style.transform=`translate(${x}px,${y}px)`}));$$('.btn, .back, .command').forEach(el=>el.addEventListener('pointerleave',()=>el.style.transform=''));
})();